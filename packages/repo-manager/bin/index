#! /usr/bin/env node
const process = require('process')
process.argv[0] = 'repo-manager'
require('./main').main().then(
  status => process.exit(status),
  error => {
    console.error(error)
    process.exit(-1)
  }
)
